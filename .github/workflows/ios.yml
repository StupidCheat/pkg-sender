name: iOS IPAA

on:
  workflow_dispatch:
  push:
    paths:
      - 'ios/**'
      - 'LoopDPI.Core/**'
      - '.github/workflows/ios.yml'

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Install iOS workload
        run: dotnet workload install ios

      - name: Build unsigned IPA
        run: >
          dotnet publish ios/PkgSender.iOS.csproj -c Release -r ios-arm64
          /p:BuildIpa=true /p:CodesignKey="-"
          /p:CheckEolWorkloads=false
          --nologo

      - name: Rename IPA
        run: |
          VER=$(sed -n 's/.*<ApplicationDisplayVersion>\([^<]*\)<.*/\1/p' ios/PkgSender.iOS.csproj | head -1); VER=${VER:-1.0.3}
          cp ios/bin/Release/net8.0-ios/ios-arm64/publish/*.ipa "PkgSender-${VER}-ios-unsigned.ipa"
          ls -la *.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: 'PkgSender-*-ios-unsigned.ipa'
